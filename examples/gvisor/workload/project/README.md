# Sample app

Fix `deadlineAfter(start, duration)` in `src/deadline.js`; it returns deadlines too early.
Keep tests and dependencies unchanged. Run `node --test`.

Uses `ms@2.1.3`, with no transitive dependencies or install scripts.
